vamos a crear la carpeta docs en la raiz del proyecto donde haremos referencia a nuestro proyecto de backend
  que esta en la carpeta C:\Users\Henry\dev\codigo-vibecoding-g2\logistica-api, dentro de backend tenemos 8 
  modulos + los auth endpoint que estan el raiz del proyecto, buscamos crear la app frontend de ese backend, 
  siguiendo las buenas practicas de desarrollo y la metologia SDD, para este primero paso analiza los endpoint 
  de backend, analiza los modulos y crea los archivos necesarios para que que este proyecto de front tenga todo
  el contexto necesario, luego una vez terminez actualiza el claude.md para que siempre tengamos contexto de 
  los modulos de back